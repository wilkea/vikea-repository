@model dpcleague_2.Models.RosterEveniment

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>RosterEveniment</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="RosterEvenimentId" />
            <div class="form-group">
                <label asp-for="EvenimentId" class="control-label"></label>
                <select asp-for="EvenimentId" class="form-control" asp-items="ViewBag.EvenimentId"></select>
                <span asp-validation-for="EvenimentId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RosterId" class="control-label"></label>
                <select asp-for="RosterId" class="form-control" asp-items="ViewBag.RosterId"></select>
                <span asp-validation-for="RosterId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
