@model IEnumerable<dpcleague_2.Models.RosterEveniment>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Eveniment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Roster)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Eveniment.Denumire)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Roster.Disciplina)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RosterEvenimentId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RosterEvenimentId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RosterEvenimentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
