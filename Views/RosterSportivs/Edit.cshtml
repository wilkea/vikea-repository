@model dpcleague_2.Models.RosterSportiv

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>RosterSportiv</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="RosterSportivId" />
            <div class="form-group">
                <label asp-for="RosterId" class="control-label"></label>
                <select asp-for="RosterId" class="form-control" asp-items="ViewBag.RosterId"></select>
                <span asp-validation-for="RosterId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SportivId" class="control-label"></label>
                <select asp-for="SportivId" class="form-control" asp-items="ViewBag.SportivId"></select>
                <span asp-validation-for="SportivId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
